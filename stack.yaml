resolver: lts-18.6
compiler: ghc-8.10.4

packages:
- .

ghc-options:
  "$locals": -funbox-strict-fields -funfolding-use-threshold=16 -fexcess-precision -optc-O3 -optc-ffast-math -O2 # -v

# extra-deps:
# -
# extra-include-dirs: [/path/to/dir]
# extra-lib-dirs: [/path/to/dir]

allow-newer: true
